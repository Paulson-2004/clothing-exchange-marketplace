diff --git a/frontend/src/index.css b/frontend/src/index.css
index 89f2095..89173ff 100644
--- a/frontend/src/index.css
+++ b/frontend/src/index.css
@@ -1,6 +1,7 @@
 :root {
   --color-bg: #faf9f6;
   --color-surface: #ffffff;
+  --color-surface-translucent: rgba(255, 255, 255, 0.85);
   --color-surface-elevated: #f4f3ef;
   --color-surface-subtle: #f0eee9;
   --color-border: #e6e4dd;
@@ -28,6 +29,7 @@
 [data-theme="dark"] {
   --color-bg: #111110;
   --color-surface: #1a1918;
+  --color-surface-translucent: rgba(26, 25, 24, 0.85);
   --color-surface-elevated: #242220;
   --color-surface-subtle: #2d2a28;
   --color-border: #3b3834;
@@ -69,6 +71,15 @@ body {
   color: var(--color-text);
 }
 
+h1, h2, h3, h4, h5, h6 {
+  font-family: var(--font-display);
+  letter-spacing: -0.02em;
+}
+
+h1 {
+  letter-spacing: -0.04em;
+}
+
 .app-shell {
   min-height: 100vh;
   display: flex;
@@ -82,9 +93,9 @@ body {
 .status-card {
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-lg);
   padding: 2rem 2.5rem;
-  box-shadow: none;
+  box-shadow: var(--shadow-sm);
   max-width: 420px;
 }
 
@@ -114,7 +125,9 @@ body {
   align-items: center;
   justify-content: space-between;
   padding: 1.5rem 3rem;
-  background: var(--color-surface);
+  background: var(--color-surface-translucent);
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
   border-bottom: 1px solid var(--color-border);
   position: sticky;
   top: 0;
@@ -126,7 +139,7 @@ body {
   display: inline-flex;
   align-items: center;
   gap: 0.75rem;
-  font-weight: 300;
+  font-weight: 500;
   font-size: 1.75rem;
   font-family: var(--font-display);
   color: var(--color-text);
@@ -179,9 +192,14 @@ body {
 .navbar-links {
   display: flex;
   align-items: center;
-  gap: 2rem;
+  gap: 1.25rem;
   margin-left: auto;
-  margin-right: 2rem;
+  margin-right: 1.25rem;
+}
+
+.navbar-links a,
+.navbar-user {
+  white-space: nowrap;
 }
 
 .navbar-links a {
@@ -209,7 +227,7 @@ body {
   gap: 1.25rem;
 }
 
-@media (max-width: 768px) {
+@media (max-width: 1100px) {
   .navbar {
     flex-wrap: wrap;
     padding: 0.85rem 1.25rem;
@@ -290,7 +308,7 @@ body {
     justify-content: center;
     text-align: center;
     padding: 0.55rem 0.75rem;
-    border-radius: 0;
+    border-radius: var(--radius-md);
     font-size: 0.92rem;
     font-weight: 500;
     text-decoration: none;
@@ -328,9 +346,9 @@ body {
 /* --- Buttons --- */
 .btn {
   border: 1px solid transparent;
-  border-radius: 0;
-  padding: 0.75rem 1.5rem;
-  font-size: 0.875rem;
+  border-radius: var(--radius-md);
+  padding: 0.6rem 1.25rem;
+  font-size: 0.9rem;
   cursor: pointer;
   text-decoration: none;
   display: inline-flex;
@@ -339,18 +357,22 @@ body {
   font-family: var(--font-sans);
   font-weight: 500;
   transition: all var(--transition-base);
-  letter-spacing: 0.05em;
-  text-transform: uppercase;
 }
 
-.btn:active {
-  transform: translateY(1px);
+.btn:active:not(:disabled) {
+  transform: scale(0.98);
+}
+
+.btn:focus-visible {
+  outline: none;
+  box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-primary);
 }
 
 .btn-primary,
 a.btn-primary {
   background-color: var(--color-primary);
   color: #ffffff;
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.15), 0 1px 2px rgba(0, 0, 0, 0.1);
 }
 
 .btn-primary:hover,
@@ -386,12 +408,11 @@ a.btn-primary:disabled {
 }
 
 .auth-form {
-  background: var(--color-surface);
+  background: transparent;
   border: none;
-  border-radius: 0;
-  padding: 3rem 2.5rem;
+  padding: 2rem 0;
   width: 100%;
-  max-width: 480px;
+  max-width: 360px;
   box-shadow: none;
   display: flex;
   flex-direction: column;
@@ -400,7 +421,7 @@ a.btn-primary:disabled {
 .auth-form h1 {
   margin: 0 0 2rem 0;
   font-size: 2rem;
-  font-weight: 300;
+  font-weight: 500;
   color: var(--color-text);
   font-family: var(--font-display);
   letter-spacing: -0.02em;
@@ -416,22 +437,20 @@ a.btn-primary:disabled {
 
 .auth-form label {
   display: block;
-  font-size: 0.75rem;
+  font-size: 0.85rem;
   font-weight: 500;
   margin-top: 0;
-  margin-bottom: 0.5rem;
-  color: var(--color-text-secondary);
-  text-transform: uppercase;
-  letter-spacing: 0.05em;
+  margin-bottom: 0.4rem;
+  color: var(--color-text);
 }
 
 .auth-form input {
   display: block;
   width: 100%;
-  height: 48px;
+  height: 42px;
   padding: 0.75rem 1rem;
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-size: 1rem;
   font-family: inherit;
   color: var(--color-text);
@@ -444,7 +463,8 @@ a.btn-primary:disabled {
   margin-bottom: 1.15rem;
 }
 
-.auth-form input::placeholder {
+input::placeholder,
+textarea::placeholder {
   color: var(--color-muted);
   opacity: 0.85;
 }
@@ -452,7 +472,7 @@ a.btn-primary:disabled {
 .auth-form input:focus {
   outline: none;
   border-color: var(--color-primary);
-  box-shadow: 0 0 0 3px rgba(47, 111, 79, 0.18);
+  box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-primary);
 }
 
 .auth-form .form-section-title {
@@ -533,7 +553,7 @@ a.btn-primary:disabled {
   background-color: var(--color-danger-light);
   color: var(--color-danger);
   padding: 0.6rem 0.8rem;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-size: 0.9rem;
   border: 1px solid rgba(181, 68, 59, 0.2);
 }
@@ -567,7 +587,7 @@ a.btn-primary:disabled {
 .page-container > h1 {
   font-family: var(--font-display);
   font-size: 2.25rem;
-  font-weight: 300;
+  font-weight: 500;
   margin: 0 0 2rem 0;
   color: var(--color-text);
   letter-spacing: -0.02em;
@@ -725,7 +745,7 @@ a.btn-primary:disabled {
 
 .how-it-works-title {
   font-size: 1.75rem;
-  font-weight: 300;
+  font-weight: 500;
   margin-bottom: 3rem;
   font-family: var(--font-display);
   letter-spacing: -0.02em;
@@ -800,27 +820,27 @@ a.btn-primary:disabled {
 }
 
 .marketplace-hero-title {
-  font-size: 2.5rem;
-  font-weight: 300;
-  letter-spacing: -0.02em;
-  margin: 0 0 1rem;
+  font-size: 2.25rem;
+  font-weight: 500;
+  letter-spacing: -0.04em;
+  line-height: 1.05;
+  margin: 0 0 1.25rem;
   color: var(--color-text);
-  line-height: 1.1;
   font-family: var(--font-display);
 }
 
 .marketplace-hero-desc {
-  font-size: 1rem;
+  font-size: 1.1rem;
   color: var(--color-text-secondary);
-  margin: 0 0 1.5rem;
-  line-height: 1.5;
-  max-width: 600px;
+  margin: 0 0 2rem;
+  line-height: 1.6;
+  max-width: 500px;
 }
 
 .marketplace-hero-cta {
   white-space: nowrap;
   flex-shrink: 0;
-  padding: 0.75rem 1.5rem;
+  padding: 0.85rem 1.75rem;
   font-size: 0.95rem;
 }
 
@@ -905,7 +925,7 @@ a.btn-primary:disabled {
 .listing-filters .btn {
   padding: 0.6rem 1rem;
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-size: 0.9rem;
   color: var(--color-text);
   background-color: var(--color-surface);
@@ -960,7 +980,7 @@ a.btn-primary:disabled {
 .listing-card {
   background: var(--color-surface);
   border: none;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   overflow: hidden;
   text-decoration: none;
   color: var(--color-text);
@@ -970,12 +990,16 @@ a.btn-primary:disabled {
 }
 
 .listing-card:hover {
-  opacity: 0.9;
+  /* removed opacity */
+}
+
+.listing-card:hover .listing-card-image img {
+  transform: scale(1.04);
 }
 
 .listing-card:focus-visible {
   outline: 2px solid var(--color-text);
-  outline-offset: 4px;
+  outline-offset: 2px;
 }
 
 .listing-card-image {
@@ -991,6 +1015,7 @@ a.btn-primary:disabled {
   height: 100%;
   object-fit: cover;
   display: block;
+  transition: transform 0.4s ease;
 }
 
 .listing-card-image-placeholder {
@@ -1008,7 +1033,7 @@ a.btn-primary:disabled {
   top: 0.75rem;
   right: 0.75rem;
   padding: 0.25rem 0.75rem;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-size: 0.75rem;
   font-weight: 600;
   letter-spacing: 0.05em;
@@ -1040,19 +1065,20 @@ a.btn-primary:disabled {
   display: flex;
   align-items: center;
   flex-wrap: wrap;
-  gap: 0.4rem;
-  font-size: 0.75rem;
+  gap: 0.5rem;
+  font-size: 0.7rem;
   color: var(--color-text-secondary);
   margin: 0 0 0.5rem;
   line-height: 1.4;
   text-transform: uppercase;
   letter-spacing: 0.05em;
+  font-weight: 500;
 }
 
 .listing-card-brand {
   font-weight: 600;
   color: var(--color-text);
-  font-size: 0.8rem;
+  font-size: inherit;
 }
 
 .listing-card-dot {
@@ -1070,12 +1096,11 @@ a.btn-primary:disabled {
 
 .listing-card-body h3 {
   margin: 0 0 0.4rem;
-  font-size: 1.15rem;
-  font-weight: 300;
+  font-size: 1.05rem;
+  font-weight: 400;
   line-height: 1.3;
   color: var(--color-text);
-  font-family: var(--font-display);
-  letter-spacing: -0.01em;
+  font-family: var(--font-sans);
   display: -webkit-box;
   -webkit-line-clamp: 1;
   -webkit-box-orient: vertical;
@@ -1134,7 +1159,7 @@ a.btn-primary:disabled {
 
 .item-details-main-image {
   background: var(--color-surface-subtle);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   overflow: hidden;
   display: flex;
   align-items: center;
@@ -1157,7 +1182,7 @@ a.btn-primary:disabled {
   width: 80px;
   height: 106px;
   object-fit: cover;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   cursor: pointer;
   opacity: 0.5;
   transition: opacity 0.2s ease;
@@ -1178,7 +1203,7 @@ a.btn-primary:disabled {
   border: none;
   padding: 0;
   cursor: pointer;
-  border-radius: 0;
+  border-radius: var(--radius-md);
 }
 
 .item-details-info {
@@ -1209,10 +1234,10 @@ a.btn-primary:disabled {
 .item-details-title {
   margin: 0 0 1.5rem;
   font-size: 2.25rem;
-  line-height: 1.2;
+  line-height: 1.1;
   font-family: var(--font-display);
-  font-weight: 300;
-  letter-spacing: -0.01em;
+  font-weight: 500;
+  letter-spacing: -0.03em;
 }
 
 .item-details-value {
@@ -1302,7 +1327,7 @@ a.btn-primary:disabled {
 
 .seller-avatar {
   width: 48px;
-  height: 48px;
+  height: 42px;
   background: var(--color-surface-subtle);
   border-radius: 50%;
   display: flex;
@@ -1367,8 +1392,8 @@ a.btn-primary:disabled {
 
 .editorial-title {
   font-family: var(--font-display);
-  font-size: 2.5rem;
-  font-weight: 300;
+  font-size: 1.8rem;
+  font-weight: 500;
   letter-spacing: -0.02em;
   margin: 0 0 0.5rem 0;
   color: var(--color-text);
@@ -1414,12 +1439,10 @@ a.btn-primary:disabled {
 }
 
 .form-group label {
-  font-size: 0.8rem;
+  font-size: 0.85rem;
   color: var(--color-text);
-  font-weight: 600;
-  text-transform: uppercase;
-  letter-spacing: 0.05em;
-  margin-bottom: 0.5rem;
+  font-weight: 500;
+  margin-bottom: 0.4rem;
   display: flex;
   flex-direction: column;
   gap: 0.25rem;
@@ -1438,7 +1461,7 @@ a.btn-primary:disabled {
 .form-group textarea {
   padding: 0.75rem 1rem;
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-size: 0.95rem;
   font-family: inherit;
   color: var(--color-text);
@@ -1451,13 +1474,25 @@ a.btn-primary:disabled {
 .form-group textarea:focus {
   outline: none;
   border-color: var(--color-primary);
-  box-shadow: 0 0 0 3px rgba(47, 111, 79, 0.18);
+  box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-primary);
 }
 
 .form-group textarea {
   resize: vertical;
 }
 
+.form-group input:disabled,
+.form-group select:disabled,
+.form-group textarea:disabled,
+.auth-form input:disabled,
+.input-disabled {
+  background-color: var(--color-surface-subtle);
+  color: var(--color-muted);
+  border-color: var(--color-border);
+  cursor: not-allowed;
+  opacity: 0.8;
+}
+
 .form-row-editorial {
   display: flex;
   gap: 1.5rem;
@@ -1648,7 +1683,7 @@ a.btn-primary:disabled {
   border: none;
   border-bottom: 1px solid var(--color-border);
   padding: 1rem 0;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   box-shadow: none;
   transition: opacity 0.2s ease;
 }
@@ -1661,7 +1696,7 @@ a.btn-primary:disabled {
   width: 80px;
   height: 106px;
   object-fit: cover;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   background: var(--color-surface-subtle);
   flex-shrink: 0;
 }
@@ -1698,7 +1733,7 @@ a.btn-primary:disabled {
   padding: 0.25rem 0.75rem;
   font-size: 0.75rem;
   font-weight: 500;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   line-height: 1;
   box-shadow: none;
   width: fit-content;
@@ -1762,10 +1797,9 @@ a.btn-primary:disabled {
 
 .empty-state {
   text-align: center;
-  padding: 4rem 2rem;
-  background: var(--color-surface);
-  border: 1px solid var(--color-border);
-  border-radius: 0;
+  padding: 3rem 1.5rem;
+  background: transparent;
+  border: none;
   box-shadow: none;
   color: var(--color-text-secondary);
 }
@@ -1773,9 +1807,9 @@ a.btn-primary:disabled {
 .empty-state h3 {
   color: var(--color-text);
   margin-bottom: 0.75rem;
-  font-size: 1.5rem;
-  font-weight: 400;
-  font-family: var(--font-display);
+  font-size: 1.4rem;
+  font-weight: 500;
+  font-family: var(--font-sans);
 }
 
 .empty-state .btn {
@@ -1786,7 +1820,7 @@ a.btn-primary:disabled {
   text-align: center;
   padding: 2rem;
   background: #fbebe9;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   color: var(--color-danger);
 }
 
@@ -1883,7 +1917,7 @@ a.btn-primary:disabled {
 .swap-context-title {
   font-family: var(--font-display);
   font-size: 1.6rem;
-  font-weight: 300;
+  font-weight: 500;
   margin: 0;
   color: var(--color-text);
 }
@@ -2111,7 +2145,7 @@ a.btn-primary:disabled {
 .swap-request-form-header h3 {
   font-family: var(--font-display);
   font-size: 1.5rem;
-  font-weight: 300;
+  font-weight: 500;
   margin: 0;
 }
 
@@ -2282,7 +2316,7 @@ a.btn-primary:disabled {
   height: 80vh;
   min-height: 500px;
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
 }
 
 @media (max-width: 720px) {
@@ -2448,7 +2482,7 @@ a.btn-primary:disabled {
   flex-wrap: wrap;
   background: var(--color-surface-subtle);
   padding: 0.6rem 0.85rem;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   border: 1px solid var(--color-border);
   margin-top: 0.4rem;
 }
@@ -2577,7 +2611,7 @@ a.btn-primary:disabled {
   resize: none;
   padding: 0.6rem 0.75rem;
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-family: inherit;
   font-size: 0.92rem;
   max-height: 100px;
@@ -2599,7 +2633,7 @@ a.btn-primary:disabled {
 .matches-section h2 {
   font-family: var(--font-display);
   font-size: 1.75rem;
-  font-weight: 300;
+  font-weight: 500;
   letter-spacing: -0.02em;
   margin-bottom: 0.5rem;
   color: var(--color-text);
@@ -2723,7 +2757,7 @@ a.btn-primary:disabled {
 
 .stats-card {
   background: var(--color-surface);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 1.25rem;
   box-shadow: var(--shadow-sm);
   display: flex;
@@ -2774,7 +2808,7 @@ a.btn-primary:disabled {
   flex: 1;
   padding: 0.45rem 0.75rem;
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-family: inherit;
   font-size: 0.9rem;
   color: var(--color-text);
@@ -2784,7 +2818,7 @@ a.btn-primary:disabled {
 .admin-filter-select {
   padding: 0.45rem 0.75rem;
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-family: inherit;
   font-size: 0.9rem;
   background: var(--color-surface);
@@ -2800,7 +2834,7 @@ a.btn-primary:disabled {
 /* Admin Tables */
 .admin-table-wrapper {
   background: var(--color-surface);
-  border-radius: 0;
+  border-radius: var(--radius-lg);
   box-shadow: var(--shadow-sm);
   border: 1px solid var(--color-border);
   overflow-x: auto;
@@ -2811,20 +2845,23 @@ a.btn-primary:disabled {
   width: 100%;
   border-collapse: collapse;
   text-align: left;
-  font-size: 0.9rem;
+  font-size: 0.85rem;
 }
 
 .admin-table th {
   background: var(--color-surface-elevated);
-  padding: 0.75rem 1rem;
-  font-weight: 600;
+  padding: 0.75rem 1.25rem;
+  font-weight: 500;
   color: var(--color-muted);
   border-bottom: 1px solid var(--color-border);
   white-space: nowrap;
+  text-transform: uppercase;
+  letter-spacing: 0.05em;
+  font-size: 0.75rem;
 }
 
 .admin-table td {
-  padding: 0.75rem 1rem;
+  padding: 1rem 1.25rem;
   border-bottom: 1px solid var(--color-border-subtle);
   vertical-align: middle;
 }
@@ -2887,7 +2924,7 @@ a.btn-primary:disabled {
 .admin-listings-page .admin-search-form,
 .admin-users-page .admin-search-form,
 .admin-swaps-page .admin-search-form {
-  max-width: 440px;
+  max-width: 400px;
 }
 
 .admin-listings-table {
@@ -3006,7 +3043,7 @@ a.btn-primary:disabled {
 /* Admin User Detail */
 .admin-detail-card {
   background: var(--color-surface);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 1.5rem;
   box-shadow: var(--shadow-sm);
   border: 1px solid var(--color-border);
@@ -3074,9 +3111,9 @@ a.btn-primary:disabled {
 .confirm-modal {
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 1.75rem;
-  max-width: 440px;
+  max-width: 400px;
   width: 100%;
   box-shadow: var(--shadow-lg);
 }
@@ -3126,8 +3163,8 @@ a.btn-primary:disabled {
   display: flex;
   align-items: center;
   justify-content: center;
-  font-size: 2.5rem;
-  font-weight: 300;
+  font-size: 1.8rem;
+  font-weight: 500;
   font-family: var(--font-display);
 }
 
@@ -3145,7 +3182,7 @@ a.btn-primary:disabled {
 .profile-title-row h1 {
   margin: 0;
   font-size: 2rem;
-  font-weight: 300;
+  font-weight: 500;
   font-family: var(--font-display);
   letter-spacing: -0.02em;
 }
@@ -3160,9 +3197,10 @@ a.btn-primary:disabled {
 .profile-card {
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
-  padding: 2rem;
+  border-radius: var(--radius-lg);
+  padding: 2.5rem;
   margin-bottom: 2rem;
+  box-shadow: var(--shadow-sm);
 }
 
 .profile-card h2 {
@@ -3232,7 +3270,7 @@ a.btn-primary:disabled {
 .profile-stat-box {
   background: var(--color-surface-subtle);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 1rem;
   text-align: center;
 }
@@ -3294,7 +3332,7 @@ a.btn-primary:disabled {
   width: 100%;
   padding: 0.65rem 0.85rem;
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   font-size: 0.95rem;
   font-family: inherit;
   color: var(--color-text);
@@ -3382,7 +3420,7 @@ a.btn-primary:disabled {
   background-color: #e7f3ec;
   color: var(--color-primary-dark);
   padding: 0.75rem 1rem;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   border: 1px solid #c3e2cf;
   font-size: 0.92rem;
 }
@@ -3409,7 +3447,7 @@ a.btn-primary:disabled {
 .dashboard-stat-card {
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 1.25rem;
   box-shadow: none;
   display: flex;
@@ -3456,9 +3494,9 @@ a.btn-primary:disabled {
 .dashboard-card {
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-lg);
   padding: 1.5rem;
-  box-shadow: none;
+  box-shadow: var(--shadow-sm);
 }
 
 .dashboard-card h2 {
@@ -3480,7 +3518,7 @@ a.btn-primary:disabled {
   padding: 0.85rem 1rem;
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   text-decoration: none;
   color: var(--color-text);
   box-shadow: none;
@@ -3499,7 +3537,7 @@ a.btn-primary:disabled {
   justify-content: center;
   width: 42px;
   height: 42px;
-  border-radius: 0;
+  border-radius: var(--radius-md);
   background: #f4f3ee;
   color: var(--color-primary);
   flex-shrink: 0;
@@ -3576,7 +3614,7 @@ a.btn-primary:disabled {
   display: inline-flex;
   align-items: center;
   gap: 0.5rem;
-  font-weight: 300;
+  font-weight: 500;
   font-size: 1.5rem;
   font-family: var(--font-display);
   color: var(--color-text);
@@ -3754,7 +3792,7 @@ a.btn-primary:disabled {
 .faq-card {
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   overflow: hidden;
   box-shadow: none;
   transition: border-color 0.15s ease, box-shadow 0.15s ease;
@@ -3833,7 +3871,7 @@ a.btn-primary:disabled {
 .faq-footer-cta {
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 2rem;
   text-align: center;
   margin-top: 3rem;
@@ -3954,7 +3992,7 @@ a.btn-primary:disabled {
   flex: 1;
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 1.25rem 1.4rem;
   box-shadow: none;
 }
@@ -4025,7 +4063,7 @@ a.btn-primary:disabled {
 .changelog-footer-cta {
   background: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 1.5rem;
   text-align: center;
   margin-top: 2.25rem;
@@ -4111,7 +4149,7 @@ a.btn-primary:disabled {
 [data-theme="dark"] textarea:focus-visible {
   border-color: var(--color-primary);
   background-color: #302b23;
-  box-shadow: 0 0 0 3px rgba(72, 163, 116, 0.28);
+  box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-primary-light);
 }
 
 [data-theme="dark"] input::placeholder,
@@ -4219,7 +4257,7 @@ a.btn-primary:disabled {
   border-top-color: var(--color-border);
 }
 
-@media (max-width: 768px) {
+@media (max-width: 1100px) {
   [data-theme="dark"] .navbar-auth-actions .navbar-login-link {
     background-color: var(--color-surface-subtle);
     color: var(--color-text) !important;
@@ -4359,10 +4397,10 @@ a.btn-primary:disabled {
 }
 
 [data-theme="dark"] .item-details-info {
-  background-color: var(--color-surface);
-  border: 1px solid var(--color-border);
-  border-radius: 0;
-  padding: 1.75rem;
+  background-color: transparent;
+  border: none;
+  border-radius: var(--radius-md);
+  padding: 0;
   box-shadow: none;
 }
 
@@ -4432,7 +4470,7 @@ a.btn-primary:disabled {
 [data-theme="dark"] .matches-section {
   background-color: var(--color-surface);
   border: 1px solid var(--color-border);
-  border-radius: 0;
+  border-radius: var(--radius-md);
   padding: 1.75rem 1.75rem 2rem;
   box-shadow: none;
   margin-top: 2.5rem;
@@ -4487,8 +4525,8 @@ a.btn-primary:disabled {
 
 /* Authentication & Profile */
 [data-theme="dark"] .auth-form {
-  background-color: var(--color-surface);
-  border-color: var(--color-border);
+  background-color: transparent;
+  border: none;
   box-shadow: none;
 }
 
@@ -4506,14 +4544,10 @@ a.btn-primary:disabled {
   color: var(--color-text);
 }
 
-[data-theme="dark"] .auth-form input::placeholder {
-  color: var(--color-muted);
-}
-
 [data-theme="dark"] .auth-form input:focus {
   border-color: var(--color-primary);
   background-color: #302b23;
-  box-shadow: 0 0 0 3px rgba(72, 163, 116, 0.28);
+  box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-primary-light);
 }
 
 [data-theme="dark"] .auth-form .form-section-title {
@@ -4867,7 +4901,7 @@ a.btn-primary:disabled {
 [data-theme="dark"] .message-input-row textarea:focus {
   background-color: var(--color-surface-subtle);
   border-color: var(--color-primary);
-  box-shadow: 0 0 0 3px rgba(72, 163, 116, 0.28);
+  box-shadow: 0 0 0 2px var(--color-bg), 0 0 0 4px var(--color-primary-light);
 }
 
 /* Admin Panel */
@@ -5233,8 +5267,8 @@ a.btn-primary:disabled {
   background-color: var(--color-primary-dark);
   color: var(--color-surface);
   font-family: var(--font-display);
-  font-size: 4rem;
-  font-weight: 300;
+  font-size: 1.8rem;
+  font-weight: 500;
   display: flex;
   align-items: center;
   justify-content: center;
@@ -5248,8 +5282,8 @@ a.btn-primary:disabled {
 
 .profile-name {
   font-family: var(--font-display);
-  font-size: 2.5rem;
-  font-weight: 300;
+  font-size: 1.8rem;
+  font-weight: 500;
   color: var(--color-text);
   margin: 0 0 0.5rem 0;
   letter-spacing: -0.02em;
@@ -5297,7 +5331,7 @@ a.btn-primary:disabled {
 .editorial-section-title {
   font-family: var(--font-display);
   font-size: 1.75rem;
-  font-weight: 300;
+  font-weight: 500;
   color: var(--color-text);
   margin: 0;
   letter-spacing: -0.01em;
@@ -5377,7 +5411,7 @@ a.btn-primary:disabled {
   .profile-avatar-giant {
     width: 100px;
     height: 100px;
-    font-size: 3rem;
+    font-size: 2rem;
   }
   
   .profile-name {
@@ -5546,8 +5580,8 @@ a.btn-primary:disabled {
 
 .overview-stat-value {
   font-family: var(--font-display);
-  font-size: 2.5rem;
-  font-weight: 300;
+  font-size: 1.8rem;
+  font-weight: 500;
   color: var(--color-text);
   line-height: 1;
   margin-bottom: 0.75rem;
@@ -5638,7 +5672,7 @@ a.btn-primary:disabled {
   gap: 5rem;
 }
 
-@media (max-width: 1024px) {
+@media (max-width: 1100px) {
   .dashboard-layout-grid {
     grid-template-columns: 1fr;
     gap: 3rem;
@@ -5705,8 +5739,8 @@ a.btn-primary:disabled {
 
 .admin-metric-value {
   font-family: var(--font-display);
-  font-size: 3.5rem;
-  font-weight: 300;
+  font-size: 2rem;
+  font-weight: 500;
   color: var(--color-text);
   line-height: 1;
 }
@@ -5767,7 +5801,7 @@ a.btn-primary:disabled {
 .admin-status-count {
   font-family: var(--font-display);
   font-size: 1.5rem;
-  font-weight: 300;
+  font-weight: 500;
   color: var(--color-text);
 }
 
@@ -5841,7 +5875,7 @@ a.btn-primary:disabled {
   color: var(--color-primary-dark);
 }
 
-@media (max-width: 1024px) {
+@media (max-width: 1100px) {
   .admin-dashboard-grid {
     grid-template-columns: 1fr;
     gap: 3rem;
@@ -6207,3 +6241,12 @@ a.btn-primary:disabled {
 
 
 
+
+
+
+
+
+
+
+
+
